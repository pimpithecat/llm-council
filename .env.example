# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Redis Configuration (for background job processing)
# OPTIONAL: Only customize if default port conflicts with your system
# Default: localhost:6380 (automatically used if not specified)
# REDIS_HOST=localhost
# REDIS_PORT=6380

# Note: Frontend runs on port 5173 (Vite default)
# Note: Backend runs on port 8001 (configured in backend/main.py)
